import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#4F63AC",
        paddingHorizontal: 8,
        paddingVertical: 20,
        width: "100%",
        borderRadius: 8,
    },
    title: {
        color: "white",
        textAlign: "center",
        fontSize: 16,
        fontWeight: "bold"
    }
});